# ecorisk 0.3.1
-   Decrease tun times of examples in plotting functions. 

# ecorisk 0.3.0
-   Add function `plot_diagnostic_sensitivity()` to create diagnostic plots of
    GAMs used in `model_sensitivity()`.
    
-   `plot_radar()` no longer fails, when only one pressure is plotted, since the
    curved text was removed.
    

# ecorisk 0.2.1

# ecorisk 0.2.0

# ecorisk 0.1.1

# ecorisk 0.1.0

-   Initial CRAN submission.

